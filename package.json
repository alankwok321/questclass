{
  "name": "teaching-app",
  "version": "1.0.0",
  "description": "Gamified AI teaching app prototype",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "seed:firestore": "node scripts/seed-firestore.js",
    "build": "cd web && npm ci --include=dev && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
